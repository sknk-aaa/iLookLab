# iLookLab 設計書（MVP版）

## 0. サービス情報

- **サービス名**: iLookLab
- **サブタイトル**: アイコン見え方ラボ - ホーム画面シミュレーター
- **ドメイン**: `ilook-lab.vercel.app`（ハイフンなし `ilooklab.vercel.app` も候補）
- **ターゲット**: 日本の個人アプリ開発者
- **告知チャネル**: X（個人開発界隈）

## 1. プロダクト概要

### 1.1 目的
モバイルアプリ開発者が、アプリのアイコンと名前が実際のiPhoneホーム画面でどう見えるかを、ブラウザ上で簡単に確認できるツール。

### 1.2 解決する課題
- アイコン単体だと、実際のホーム画面での見え方（他アプリとの並び、視認性、背景との相性）がわからない
- 公式のシミュレータやデバイスに入れるのは手間
- デザイナーや関係者への共有時に「実際のホーム画面イメージ」を素早く見せたい

### 1.3 差別化ポイント
- 類似ツール（found-tools等）はアイコン単体のマスク確認のみ
- 本ツールはホーム画面に並んだ状態で確認できる点が独自
- マスク（iOS Squircle）も自動適用される

### 1.4 プラットフォーム戦略
- **Phase 1（本MVP）**: Web版、iOSのみ対応
- **Phase 2以降**: Android対応、背景変更、並び替え、モバイルアプリ化など

---

## 2. 機能要件（MVP）

### 2.1 機能リスト

| # | 機能 | 説明 |
|---|------|------|
| F1 | アイコン画像アップロード | PNG/JPG/WebP/SVG。ドラッグ&ドロップとファイル選択 |
| F2 | アプリ名入力 | テキスト入力、リアルタイム反映 |
| F3 | iOSホーム画面プレビュー | 用意した1枚の背景画像の上に、アイコンと名前を1箇所だけ重ねる |
| F4 | PNGダウンロード | 完成プレビューを書き出し |

### 2.2 スコープ外（MVPでは作らない）
- Android対応
- 背景壁紙の選択・切り替え
- アイコンの並び替え
- 周辺アプリの編集
- 複数アイコン表示
- ユーザーログイン
- データ保存

---

## 3. ユーザーフロー

```
[サイト訪問]
    ↓
[アイコン画像をアップロード]
    ↓
[アプリ名を入力]
    ↓
[プレビューを確認]
    ↓
[ダウンロード]
```

すべて1画面で完結。リアルタイムプレビュー。

---

## 4. 画面構成

### 4.1 全体レイアウト
- **基調色**: ライト基調
- **最大幅**: 1440px、中央寄せ
- **左右ペイン比率**: 4 : 6

```
┌──────────────────────────────────────────────────────────┐
│  ヘッダー（iLookLab ロゴ / サブタイトル）                  │
├────────────────┬─────────────────────────────────────────┤
│                │                                          │
│   左ペイン      │      右ペイン                            │
│   (4)          │      (6)                                 │
│                │                                          │
│  1. アイコン    │   ┌──────────────┐                       │
│     アップロード │   │              │                       │
│                │   │  iPhone       │                       │
│  2. アプリ名    │   │  プレビュー    │                       │
│                │   │              │                       │
│  [ダウンロード]  │   └──────────────┘                       │
│                │                                          │
└────────────────┴─────────────────────────────────────────┘
```

レスポンシブで、モバイル幅（〜768px）では縦並びに切り替わる。

### 4.2 左ペイン（操作エリア）
上から順に：
1. **アイコン**: ドロップゾーン＋プレビュー表示
2. **アプリ名**: テキスト入力（プレースホルダー「アプリ名」、デフォルト値は空）
3. **ダウンロードボタン**: 目立つ色

### 4.3 右ペイン（プレビューエリア）
- 用意した1枚の背景画像（iPhone本体フレーム＋ホーム画面UI＋他アプリ＋破線枠）を表示
- 破線枠の位置に、アップロードされたアイコンを重ねて描画
- 破線枠の下に、入力されたアプリ名を重ねて描画

---

## 5. 技術スタック

| レイヤ | 技術 | 理由 |
|--------|------|------|
| フレームワーク | Next.js (App Router) | SSG/SSRどちらも可、デプロイ容易 |
| 言語 | TypeScript | 型安全 |
| スタイリング | Tailwind CSS | ツール系UIに向く、開発速度 |
| 画像書き出し | html-to-image | DOMをPNG化 |
| ホスティング | Vercel | Next.jsと相性◎、無料枠で十分 |

### 5.1 ディレクトリ構成

```
/app
  /page.tsx              # メイン画面
  /layout.tsx
/components
  IconUploader.tsx       # アップローダー
  AppNameInput.tsx       # 名前入力
  Preview.tsx            # プレビュー（背景画像 + 重ねるレイヤー）
  DownloadButton.tsx     # ダウンロード
/lib
  /utils
    exportImage.ts       # html-to-image ラッパー
    truncateName.ts      # アプリ名の8文字省略
  constants.ts           # 配置座標などの定数
/public
  /assets
    iphone-bg.png        # 背景画像（こちらで用意）
```

---

## 6. データ設計（クライアントステート）

```typescript
type AppState = {
  iconImage: string | null;  // dataURL（アップロード画像）
  appName: string;           // ユーザー入力（デフォルト: 空）
};
```

シンプル。`useState` で管理。

---

## 7. 詳細仕様

### 7.1 背景画像
- ファイル: `/public/assets/iphone-bg.png`
- 内容: iPhoneフレーム + iOSホーム画面UI + 他アプリアイコン + 「ここにアイコンが入る」破線枠
- 破線枠の位置: 5行目2列目（My Appの右隣）
- この画像はこちらで用意する

### 7.2 アイコン重ね合わせ

| 項目 | 仕様 |
|------|------|
| 配置位置 | 背景画像の破線枠の中心 |
| 座標 | 背景画像のピクセル座標で定数化（`lib/constants.ts`） |
| サイズ | 背景画像の他アイコンと同サイズ |
| マスク | iOS Squircle（半径22.37%）。SVG clipPath で実装、または `figma-squircle` ライブラリ使用 |

### 7.3 アプリ名表示

| 項目 | 仕様 |
|------|------|
| 位置 | アイコン直下、中央揃え |
| アイコンとの間隔 | 約5px（背景画像の他アプリ名との間隔に合わせる） |
| フォント | SF Pro |
| 色 | 白 |
| 影 | 軽い text-shadow（白文字を壁紙の上で読みやすくするため） |
| サイズ | 背景画像の他アプリ名と同サイズ（約13px相当） |
| 行数 | 1行固定 |
| 文字数制限 | 8文字目以降は「…」で省略（半角・全角問わず） |
| 入力欄プレースホルダー | 「アプリ名」 |
| デフォルト値 | 空（空のときはプレビューにも名前を表示しない） |

### 7.4 アイコンアップロード
- 対応形式: PNG, JPG, WebP, SVG
- 推奨サイズ案内: 512×512px以上
- アップロード後はクライアント側でのみ保持（サーバー送信なし）
- 元画像は四角でOK、Squircleマスクは表示時にCSS/SVGで適用

### 7.5 ダウンロード
- ボタン1つでPNG書き出し
- 対象: 右ペインのプレビュー全体（背景画像 + 重ねたアイコン + 名前）
- 解像度: 表示の2〜3倍（Retina対応）
- ファイル名例: `homescreen-{appname}.png`（名前が空なら `homescreen.png`）

---

## 8. フォント

| 用途 | フォント | ライセンス |
|------|---------|-----------|
| サイト本体UI（日本語） | Noto Sans JP（Google Fonts） | SIL Open Font License、フリー |
| サイト本体UI（英数字） | Inter（Google Fonts） | SIL Open Font License、フリー |
| iOSプレビュー内のアプリ名表示 | SF Pro（Appleライセンス同意） | iOSモックアップ用途で使用OK |

---

## 9. 非機能要件

| 項目 | 要件 |
|------|------|
| パフォーマンス | アイコン変更→プレビュー反映100ms以内 |
| ブラウザ対応 | Chrome / Safari / Firefox / Edge の最新版 |
| レスポンシブ | PC（メイン）、タブレット、スマホ縦持ち |
| プライバシー | アップロード画像はサーバーに送信しない |
| SEO | OGP設定、メタタグ整備 |

---

## 10. Claude Code への依頼内容

### 10.1 含めるもの
- Next.js (App Router) + TypeScript + Tailwind CSS のプロジェクト初期化
- メイン画面1ページ（`/app/page.tsx`）
- 左右2ペイン構成（左4 : 右6）、ライト基調、最大幅1440px
- ヘッダー: 「iLookLab」ロゴ + サブタイトル「アイコン見え方ラボ - ホーム画面シミュレーター」
- 左ペイン:
  - アイコン画像アップロード（クライアント完結、dataURL化、ドラッグ&ドロップ対応）
  - アプリ名入力（プレースホルダー「アプリ名」、デフォルト空）
  - ダウンロードボタン（PNG書き出し、html-to-image使用）
- 右ペイン:
  - 背景画像 `/public/assets/iphone-bg.png` を表示
  - 背景画像内の破線枠の位置に、アップロードされたアイコンを重ねる
    - iOS Squircleマスク（半径22.37%）を適用
    - サイズは背景画像の他アイコンと同サイズ
  - 破線枠の下に、入力されたアプリ名を重ねる
    - フォント: SF Pro、色: 白、軽い影、約13px
    - 8文字目以降は「…」で省略
    - 名前が空なら何も表示しない
- リアルタイム反映

### 10.2 含めないもの（次フェーズ）
- Android対応
- 背景画像の切替
- 並び替え機能
- 周辺アプリの編集
- 完璧なレスポンシブ対応

### 10.3 技術的な指示
- 状態管理: Reactの `useState` で十分（Zustand等は不要）
- Squircle実装: `figma-squircle` または SVG `clipPath` で手書きパス
- ファイルアップロード: `<input type="file">` + `FileReader` で dataURL に変換
- スタイル: Tailwind の素のクラスでOK
- フォント:
  - サイトUI: Noto Sans JP + Inter（Google Fontsから読み込み）
  - プレビュー内のアプリ名: SF Pro（Appleからダウンロードして同梱）
- 背景画像内の破線枠の位置（アイコン配置座標とサイズ）は、`/public/assets/iphone-bg.png` を見て読み取り、`lib/constants.ts` に定数として定義する
  ```typescript
  // 例
  export const ICON_POSITION = {
    centerX: 0.35,  // 背景画像幅に対する相対位置(0〜1)
    centerY: 0.56,
    sizeRatio: 0.18, // 背景画像幅に対するアイコンのサイズ比
  };
  ```

### 10.4 完成イメージ
ローカルで `npm run dev` を実行し、アイコンをアップロード → 名前を入力 → 右ペインのプレビューでリアルタイム反映 → ダウンロードボタンでPNG保存、までが動けばOK。

### 10.5 完成後のレビューポイント
- レイアウトの破綻がないか（4:6比率、1440px最大）
- アイコンが破線枠の位置にきれいに収まっているか
- Squircleマスクが正しく適用されているか
- アプリ名のフォント・色・サイズが背景画像の他アプリと馴染んでいるか
- 8文字超過時の「…」省略が動いているか
- PNGダウンロードが正しく動くか

---

## 11. 未決事項

- [ ] ロゴ・カラーテーマ
- [ ] 利用規約・プライバシーポリシー
- [ ] 背景画像から「My App」テキストを削除するかどうか（現状は残したまま、破線枠だけ追加した版を使用）
